velocidade_registrada = float(input("Digite a velocidade registrada: "))

if velocidade_registrada > 80:
    print("A velocidade registrada está acima do limite permitido.")
else:
    print("A velocidade registrada está dentro do limite permitido.")